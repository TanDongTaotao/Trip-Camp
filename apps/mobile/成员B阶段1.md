# 携程训练营 - 移动端 (Phase 1)

本项目是携程训练营的移动端部分，基于 **Taro + React** 开发。
阶段 1 主要完成了基础架构搭建、请求封装以及首页/列表/详情页的联调准备。

## 🛠️ 技术栈
- **框架**: Taro v4 (React)
- **UI 组件**: @tarojs/components
- **样式**: SCSS (Sass)
- **构建目标**: H5 (Web) / 微信小程序

## 🚀 快速开始

### 1. 安装依赖
在 `apps/mobile` 目录下执行：
```bash
npm install
```

### 2. 启动开发服务 (H5)
```bash
npm run dev:h5
```
启动成功后，浏览器访问 `http://localhost:10086/`。

> 注意：需要先启动后端服务 (`server` 目录 `npm run dev`) 才能正常请求接口数据。

## 📝 阶段 1 功能说明

### 1. 统一请求封装 (`utils/request.js`)
- **BaseURL**: 默认指向 `http://localhost:3000/api/v1`。
- **全局错误处理**: 拦截非 2xx 响应，自动弹出 Toast 提示错误信息。

### 2. 页面结构准备 (`pages/home` / `pages/list` / `pages/detail`)
- 首页用于搜索入口与日期/城市筛选。
- 列表页用于承接酒店搜索结果。
- 详情页用于展示酒店详情信息。

## 📂 目录结构 (部分)
```
apps/mobile/src
├── pages
│   ├── home/           # 首页
│   ├── list/           # 列表页
│   └── detail/         # 详情页
├── utils
│   └── request.js      # 请求封装核心
├── app.config.js       # 全局配置 (路由等)
└── app.js              # 入口文件
```
