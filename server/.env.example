PORT=3000
NODE_ENV=development

# 作业/联调方便：允许注册管理员账号（生产环境不要开启）
ALLOW_ADMIN_REGISTER=true

# MongoDB Atlas（把 <username>/<password> 替换成你自己的）
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.ritrxtz.mongodb.net/tripcamp?retryWrites=true&w=majority&appName=Cluster0

# JWT（换成一段足够长的随机字符串）
JWT_SECRET=please_change_me_to_a_long_random_string
